# Shipped invalid string-literal assignment case
Surface: shell + compiler CLI
Invocation: sed -n '1,120p' tests/tests/chapter_16/invalid_types/assign_to_string_literal.c
int main(void) {
    // you can't assign to a string literal;
    // it decays to a pointer to string's first element, which isn't an lvalue
    "foo" = "bar";
    return 0;
}
Invocation: ./target/release/rustcc tests/tests/chapter_16/invalid_types/assign_to_string_literal.c
type error: cannot assign to array object
compile_exit=1
