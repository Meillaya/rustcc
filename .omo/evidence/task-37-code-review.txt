Task 37 code-review / slop scope-fidelity report
=================================================

Review target
-------------
- Functional implementation commit inspected: dfd5d919349bf52148b1f95401f7b124d8583497 (`feat(compiler): chapter 13: double floating-point core`).
- This artifact is evidence-only for task 37. It does not change product source, plan checkboxes, Boulder state, or final verification wave state.
- Inspection commands used for this report:
  - `git status --short`
  - `git log --oneline -5`
  - `git show --stat --name-status --format=fuller --no-renames dfd5d919349bf52148b1f95401f7b124d8583497`
  - `git diff --name-only dfd5d919349bf52148b1f95401f7b124d8583497^ dfd5d919349bf52148b1f95401f7b124d8583497 -- src docs .omo/evidence/task-37-*`
  - `git diff --unified=4 dfd5d919349bf52148b1f95401f7b124d8583497^ dfd5d919349bf52148b1f95401f7b124d8583497 -- src/lex/scanner.rs src/parse/parser.rs src/codegen/abi.rs src/codegen/assembly.rs src/codegen/codegen.rs src/codegen/emit.rs src/codegen/fixup.rs src/codegen/replace_pseudos.rs`
  - `rg -n "NaN|nan|unordered|jp|parity|setp|setnp" src || true`
  - `rg -n "aggregate|struct|union|Sse|XMM_PARAM_REGS|classify_params|Double" src/codegen src/ir src/parse src/semantics || true`

Changed-surface summary
-----------------------
- Task-37 commit changed Chapter 13 double pipeline files only: scanner/parser, semantic resolve, TACKY lowering, System V AMD64 scalar double codegen/fixup/emit/pseudo replacement, compiler orchestration, task evidence, and coaching log.
- `git diff --name-only` for the task-37 commit listed only:
  - `.omo/evidence/task-37-baseline.txt`
  - `.omo/evidence/task-37-ch13-core-gate.txt`
  - `.omo/evidence/task-37-manual-qa.txt`
  - `.omo/evidence/task-37-regressions.txt`
  - `docs/COACHING_LOG.md`
  - `src/codegen/{abi.rs,assembly.rs,codegen.rs,emit.rs,fixup.rs,replace_pseudos.rs}`
  - `src/compiler.rs`
  - `src/ir/lower.rs`
  - `src/lex/scanner.rs`
  - `src/parse/parser.rs`
  - `src/semantics/resolve.rs`
- No product files are modified by this report commit; it adds only task-37 evidence files.

Programming / remove-ai-slops overfit coverage
-----------------------------------------------
1. No NaN extra
   - PASS. Commit body explicitly records `Task 38 owns NaN-aware comparison semantics and --nan coverage` and rejects implementing unordered NaN behavior in task 37.
   - Source search for `NaN|nan|unordered|jp|parity|setp|setnp` found no task-37 NaN feature implementation paths. The only relevant source hits are comments/reserved condition-code documentation (for example `src/codegen/assembly.rs` notes parity/unordered semantics and `src/ir/tacky.rs` reserves parity codes), not executable NaN handling.
   - Existing task evidence also records `Not-tested: chapter 13 --nan extras`, keeping NaN outside the success claim.

2. No later features
   - PASS. The implementation scope is Chapter 13 core double support: floating constants, `double` type parsing, scalar arithmetic/comparisons/logical conversions, double static init, function call/return handling, XMM register passing, and int/double conversion instructions.
   - No task-38+ feature was claimed or implemented in this evidence pass. In particular, no NaN suite, aggregate/struct double ABI, or long-double path is added.
   - Chapter 12 residual failures are documented as nonblocking/pre-existing in `.omo/evidence/task-37-regressions.txt`; this report does not reinterpret them as task-37 success.

3. No speculative abstraction
   - PASS. The added code follows existing compiler stages rather than introducing a new framework layer: lexer tokenization, parser type-specifier handling, resolver retention of `DoubleConstant`, TACKY lowering conversions, ABI classification, codegen, pseudo replacement, fixup, and emit.
   - The new `CodegenCtx` is local to codegen for concrete needs (double constant labels and temporary labels). It is not a general-purpose abstraction or cross-cutting architecture change.
   - ABI changes extend the existing `ParamClass` classification with concrete integer/SSE register indices; this is a minimal scalar-register accounting change required by mixed integer/double calls.

4. No stale or misleading success evidence
   - PASS. This report separates three evidence categories:
     - historical task-37 implementation evidence committed in dfd5d919349bf52148b1f95401f7b124d8583497,
     - verifier context noting functional criteria passed but this slop artifact was missing,
     - fresh gate reruns saved after this report in `.omo/evidence/task-37-evidence-fix-gate.txt`.
   - The report does not claim chapter 12 cleanup, NaN extras, or aggregate ABI as successful. It preserves the known nonblocking exclusions from the task-37 commit trailers.

5. Malformed input probes
   - PASS. Existing evidence includes malformed floating literal coverage: `.omo/evidence/task-37-manual-qa.txt` records `lex error: malformed floating constant 1e`.
   - Scanner review shows malformed exponent and invalid float suffix paths remain explicit (`lex error: malformed floating constant ...`, `lex error: invalid floating constant suffix ...`).
   - Parser review shows invalid specifier combinations are rejected (`double` cannot combine with `int`/`long`/`unsigned`/`signed`; duplicate `double`, `signed`, `unsigned`, and `long` are rejected).

6. Scope-fidelity probes
   - PASS. The task-37 commit records Chapter 13 core gate success and Chapter 5 latest-only regression probe success; existing evidence includes `.omo/evidence/task-37-ch13-core-gate.txt` and `.omo/evidence/task-37-manual-qa.txt`.
   - Scope checks reviewed:
     - Chapter 13 latest-only core gate was part of the functional commit evidence.
     - Chapter 5 latest-only with bitwise/compound/increment was part of commit trailers, protecting earlier local-variable/operator scope.
     - The fresh evidence-fix gate reruns `cargo build --release`, `cargo test --release`, and `./tests/test_compiler ./target/release/rustcc --chapter 13 --latest-only` after this artifact is written.

7. No broad formatting churn
   - PASS with narrow caveat. The task-37 diff is limited to Chapter 13 double implementation files plus evidence/logging. There is no repo-wide formatting pass.
   - Focused diff review shows a few incidental formatting changes inside touched files (for example import ordering and wrapped `matches!` arms in `src/lex/scanner.rs` / `src/parse/parser.rs`), but they are local to implementation files already changed for Chapter 13 and do not constitute broad churn.

8. No beyond-SSE2 / aggregate ABI
   - PASS. ABI/codegen review is scalar System V AMD64 double only:
     - `src/codegen/abi.rs` adds `XMM_PARAM_REGS: [usize; 8]` and classifies `OperandType::Double` as `ParamClass::Sse(index)` until XMM registers are exhausted, then stack.
     - `src/codegen/codegen.rs` moves double args/returns with `Movsd` and `%xmmN`/`%xmm0`.
     - `src/codegen/assembly.rs` / `src/codegen/emit.rs` use scalar double SSE2-style operations (`movsd`, `addsd`, `subsd`, `mulsd`, `divsd`, `cvttsd2si`, `cvtsi2sd`).
   - Aggregate/struct/union ABI search found only existing Rust implementation structs and comments; no C aggregate or struct/union ABI implementation was introduced for task 37.
   - Commit trailers explicitly constrain task 37 to `SSE2 scalar double support only; no aggregate/struct double ABI` and mark aggregate double ABI as not tested.

Review conclusion
-----------------
PASS for the verifier blocker: task 37 now has an explicit code-review/slop artifact enumerating programming/remove-ai-slops overfit coverage. The artifact is intentionally evidence-only and does not broaden implementation scope.
