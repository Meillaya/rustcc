WORKING: task 42 diagnosis - mapping failures

Scope note: this is a snapshot-based fix map derived from `.omo/evidence/task-42-native-followup-failures.txt` plus the current Rust/OCaml mirror locations. The current tree appears to have advanced past some of the archived failures, but these are still the right executor touchpoints for the remaining Chapter 15 array/pointer regressions.

Bridge check: `rg -n "bridge|gcc|system-C" src` only finds the gcc invocations in `src/toolchain.rs`; there is no native bridge fallback path to revive.

## Prioritized fix map

| P | Failure cluster / tests | Likely root cause | Exact Rust location(s) | OCaml mirror location(s) | Minimal fix shape | Risk |
|---|---|---|---|---|---|---|
| 1 | Static/global array emit + nested/static initializer flattening (`initialization/static`, `initialization/static_nested`, `libraries/global_array`, parts of `initialization/automatic*`, `declarators/equivalent_declarators`) | Aggregate static initializers are flattened without preserving nested element widths, so nested arrays lose their real shape/size when they reach assembly emission. | `src/ir/lower.rs:96-118` (`static_init_for_type`), `src/codegen/codegen.rs:1548-1571` (`convert_static_init`), `src/codegen/emit.rs:441-477` (`format_static_variable`) | `nqcc2/lib/semantic_analysis/typecheck.ml:551-561, 590-633`; `nqcc2/lib/backend/codegen.ml:846-873`; `nqcc2/lib/emit.ml:301-336` | Keep recursive element typing while flattening aggregates; size zeros from the nested type, not a blanket scalar width; emit `.bss`/`.data` from the converted byte-array/width metadata. | Moderate |
| 2 | Automatic/nested array initialization and for-loop array declaration init rejection (`initialization/automatic`, `initialization/automatic_nested`, `declarators/for_loop_array`) | The local-array initializer path must recurse by element type and zero-fill the tail; the for-init declaration path must stay on the declaration validator instead of falling through to generic expression typing. | `src/ir/lower.rs:1217-1336` (`lower_array_initializer`, `zero_initialize_array`, `initialize_array_elements`, `add_const_index`), `src/semantics/typecheck.rs:130-146, 149-189` (`check_var_decl`, `check_statement`), `src/semantics/resolve.rs:593-613` (`resolve_for_init`) | `nqcc2/lib/semantic_analysis/typecheck.ml:590-633, 677-745`; `nqcc2/lib/semantic_analysis/resolve.ml:143-149` | Make the for-init declaration path reuse the array-aware declaration validator; in the lowerer, recurse on nested arrays and convert each leaf initializer by assignment before storing. | High |
| 3 | Pointer-to-array decay / scaling and subscript type handling (`casts/implicit_and_explicit_conversions`, `casts/multi_dim_casts`, `subscripting/addition_subscript_equivalence`, `subscripting/array_of_pointers_to_arrays`, `subscripting/simple_subscripts`, `subscripting/subscript_nested`, `declarators/return_nested_array`) | Subscript and pointer-arithmetic lowering must preserve pointer-to-array pointee size, and the integer-index gate must accept all integer widths (`unsigned`, `long`, `unsigned long`) before lowering. | `src/ast/ty.rs:65-69` (`decay`), `src/semantics/typecheck.rs:239-335, 467-516` (`type_of_expr`, `validate_cast`, `validate_initializer`), `src/ir/lower.rs:1178-1215, 1777-1844, 1982-1986` (`lower_subscript`, `lower_pointer_binary`, `subscript_element_type`) | `nqcc2/lib/semantic_analysis/typecheck.ml:401-443`; `nqcc2/lib/backend/codegen.ml:776-803` | Keep array decay to pointer only in value contexts, preserve pointer-to-array element type for `+`/`[]`, and use the pointee size as the `AddPtr` scale. | High |
| 4 | Stack/global array alignment (`allocation/test_alignment`) | Array locals/statics need the 16-byte alignment rule when their size crosses the threshold, and the stack-slot allocator must round the slot, not just the final frame, to that alignment. | `src/codegen/replace_pseudos.rs:34-47` (`resolve`), `src/codegen/codegen.rs:1565-1583` (`get_var_alignment`, `generate`), `src/codegen/emit.rs:441-477` (`format_static_variable`) | `nqcc2/lib/backend/replace_pseudos.ml:10-47`; `nqcc2/lib/backend/codegen.ml:846-873`; `nqcc2/lib/emit.ml:314-336` | Round `ByteArray` locals/statics to 16 when size >= 16, keep the frame-size alignment in sync, and emit the matching `.align` for statics. | Moderate |
| 5 | Static-local duplicate labels / symbol collisions (`pointer_arithmetic/pointer_add`) | Two static locals from different functions can still collapse to the same emitted label if the resolved unique name is not carried all the way through lowering/emission. | `src/semantics/resolve.rs:376-425` (`ScopeStack::new`, `declare`), `src/ir/lower.rs:471-482` (`lower_block_items` local-static emission) | `nqcc2/lib/semantic_analysis/resolve.ml:99-118, 143-149, 289-297` | Keep function-scoped mangling for all local statics and make the lowered top-level static variable name come from the resolved unique name, not the source identifier. | Low–moderate |

## Failure-to-fix mapping summary

- `alignment` → bucket 4
- `for_loop_array` → bucket 2
- `simple_subscripts` / `addition_subscript_equivalence` / `array_of_pointers_to_arrays` / `subscript_nested` / `return_nested_array` / the cast tests → bucket 3
- `automatic` / `automatic_nested` → buckets 1 + 2
- `static` / `static_nested` / `global_array` → bucket 1
- `pointer_add` duplicate-label assembler failure → bucket 5
- `equivalent_declarators` → buckets 1 + 3 + 5, depending on which declarator form is failing in the executor run

## Minimal executor order

1. Fix bucket 1 first: static/global array materialization/emit.
2. Then fix bucket 3: pointer-to-array decay and subscript scaling.
3. Then bucket 2: automatic arrays + for-init declaration path.
4. Then bucket 4: stack/global alignment.
5. Finally bucket 5: static-local label uniqueness.
