# Task 54 / W20-T4 Copy Propagation Fix Evidence

Date: 2026-07-08
Worktree: /home/mei/projects/rustcc
Scope: post-review file-size/slop fix only; no staging or commit performed.

## Summary

PASS. Task 54 copy propagation behavior remains intact after refactoring the review blockers:

- Split constant-folding static-variable side-table handling from `src/ir/constant_folding/instr.rs` into `src/ir/constant_folding/state.rs`, leaving all touched constant-folding source files below 250 pure LOC.
- Relocated Task 54 codegen helper logic out of oversized `src/codegen/codegen.rs` into `src/codegen/codegen/copy_prop_support.rs`; no blanket allow and no broad `SIZE_OK` exception was needed.
- Official `--propagate-copies` Chapter 19 gate still passes: 42 tests OK.

## Changed files for this fix

- `src/ir/constant_folding.rs` — wires static variable names into constant folding so memory-safe invalidation remains available after split.
- `src/ir/constant_folding/instr.rs` — now delegates constant table mutation/invalidations to `ConstState`; reduced below 250 pure LOC.
- `src/ir/constant_folding/folds.rs` — uses `ConstState` for remember/forget operations instead of duplicating table edits.
- `src/ir/constant_folding/state.rs` — new small helper holding constants and static-variable side table.
- `src/codegen/codegen.rs` — delegates Task 54 call-arg and const-index `AddPtr` lowering helpers to child module.
- `src/codegen/codegen/copy_prop_support.rs` — new small codegen helper module for copy-prop-induced call arg reuse and constant-index pointer arithmetic support.

Existing Task 54 files from the implementation remain in the worktree and were included in LOC accounting:

- `src/ir/copy_propagation.rs`
- `src/ir/copy_propagation/facts.rs`
- `src/ir/copy_propagation/dataflow.rs`
- `src/ir/copy_propagation/rewrite.rs`
- `src/ir/copy_propagation/cleanup.rs`
- `src/ir/mod.rs`
- `src/ir/opt.rs`
- `src/pipeline.rs`

## Pure LOC counts

Measured with:

```bash
python3 - <<'PY'
from pathlib import Path
files = [
'src/ir/constant_folding/instr.rs',
'src/ir/constant_folding/folds.rs',
'src/ir/constant_folding/state.rs',
'src/codegen/codegen.rs',
'src/codegen/codegen/copy_prop_support.rs',
'src/ir/copy_propagation.rs',
'src/ir/copy_propagation/facts.rs',
'src/ir/copy_propagation/dataflow.rs',
'src/ir/copy_propagation/rewrite.rs',
'src/ir/copy_propagation/cleanup.rs',
'src/ir/constant_folding.rs',
'src/ir/mod.rs',
'src/ir/opt.rs',
'src/pipeline.rs',
]
for f in files:
    p=Path(f)
    pure=sum(1 for line in p.read_text().splitlines() if line.strip() and not line.strip().startswith('//'))
    print(f'{pure:4d} {f}')
PY
```

Output:

```text
 234 src/ir/constant_folding/instr.rs
 166 src/ir/constant_folding/folds.rs
  21 src/ir/constant_folding/state.rs
1908 src/codegen/codegen.rs
  98 src/codegen/codegen/copy_prop_support.rs
  73 src/ir/copy_propagation.rs
 179 src/ir/copy_propagation/facts.rs
 204 src/ir/copy_propagation/dataflow.rs
 244 src/ir/copy_propagation/rewrite.rs
 110 src/ir/copy_propagation/cleanup.rs
  78 src/ir/constant_folding.rs
  13 src/ir/mod.rs
  41 src/ir/opt.rs
  92 src/pipeline.rs
```

Explanation for oversized `src/codegen/codegen.rs`: no source-level `SIZE_OK` exception added. The Task 54 helper logic that made the large file worse was moved to `src/codegen/codegen/copy_prop_support.rs` (98 pure LOC). The remaining `codegen.rs` Task 54 footprint is narrow delegation (`mod copy_prop_support;` plus helper calls) and the file is 1908 pure LOC, below the reviewer-noted HEAD baseline of 1910.

## Exact verification commands and results

All required commands were run in one shell with output captured to `/tmp/task54-fix-required-commands.log`:

```bash
set -o pipefail
{
echo '### cargo fmt --all -- --check'; cargo fmt --all -- --check
echo '### cargo check --release'; cargo check --release
echo '### cargo build --release'; cargo build --release
echo '### cargo test --release'; cargo test --release
echo '### copy prop gate'; ./tests/test_compiler ./target/release/rustcc --chapter 19 --latest-only --propagate-copies
echo '### uce gate'; ./tests/test_compiler ./target/release/rustcc --chapter 19 --latest-only --eliminate-unreachable-code
echo '### constant fold gate'; ./tests/test_compiler ./target/release/rustcc --chapter 19 --latest-only --fold-constants
echo '### chapter18 union gate'; ./tests/test_compiler ./target/release/rustcc --chapter 18 --latest-only --union
echo '### manual copy-prop probe';
cat > /tmp/rustcc_cp_probe.c <<'C'
int copy_probe(int a, int b) {
    int c = a;
    int d = c;
    return d + b;
}
int memory_probe(int *p, int v) {
    int x = v;
    *p = 9;
    return x + *p;
}
int address_probe(int a) {
    int x = a;
    int *p = &x;
    *p = 3;
    return x;
}
int main(void) {
    int n = 4;
    return copy_probe(2, 5) == 7 && memory_probe(&n, 8) == 17 && address_probe(1) == 3 ? 0 : 1;
}
C
./target/release/rustcc --tacky --fold-constants --eliminate-unreachable-code --propagate-copies /tmp/rustcc_cp_probe.c > /tmp/rustcc_cp_probe.tacky
sed -n '/name: "copy_probe"/,/return_type/p;/name: "memory_probe"/,/return_type/p;/name: "address_probe"/,/return_type/p' /tmp/rustcc_cp_probe.tacky | sed -n '1,220p'
./target/release/rustcc /tmp/rustcc_cp_probe.c
/tmp/rustcc_cp_probe; echo "manual_probe_exit=$?"
echo '### git diff --check'; git diff --check
} 2>&1 | tee /tmp/task54-fix-required-commands.log
```

Pass/fail summary:

- PASS — `cargo fmt --all -- --check`
- PASS — `cargo check --release`
- PASS — `cargo build --release`
- PASS — `cargo test --release` (`src/main.rs`: 10 passed; lib/doc: 0 tests)
- PASS — `./tests/test_compiler ./target/release/rustcc --chapter 19 --latest-only --propagate-copies` (42 tests OK)
- PASS — `./tests/test_compiler ./target/release/rustcc --chapter 19 --latest-only --eliminate-unreachable-code` (15 tests OK)
- PASS — `./tests/test_compiler ./target/release/rustcc --chapter 19 --latest-only --fold-constants` (16 tests OK)
- PASS — `./tests/test_compiler ./target/release/rustcc --chapter 18 --latest-only --union` (286 tests OK; assembler emitted pre-existing static initializer truncation warnings during negative/edge cases)
- PASS — manual copy-prop probe, `/tmp/rustcc_cp_probe` exited 0. Optimized TACKY showed the pure copy chain feeding `copy_probe` through `copy_probe.a.0`, while memory/address cases retained loads/stores/address-taking behavior.
- PASS — `git diff --check`

## Diagnostics

LSP diagnostics were checked for fix-touched Rust files and reported no diagnostics:

- `src/ir/constant_folding/instr.rs`
- `src/ir/constant_folding/folds.rs`
- `src/ir/constant_folding/state.rs`
- `src/codegen/codegen.rs`
- `src/codegen/codegen/copy_prop_support.rs`

## Risks / follow-ups

- `src/codegen/codegen.rs` remains a large legacy file, but Task 54 no longer adds helper bodies there. Further broad codegen decomposition is outside Task 54 scope and would risk unrelated behavior.
- Copy propagation remains intentionally conservative around memory, calls, stores/loads, and address-taken variables.
- No compiler-phase Rust unit tests were added, per plan constraint.
