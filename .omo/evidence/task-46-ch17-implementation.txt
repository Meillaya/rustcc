Task 46 / W18-T1 Chapter 17 implementation evidence
Date: 2026-07-08
Workspace: /home/mei/projects/rustcc

Baseline before edits
- git status --short: modified tree had no tracked changes; pre-existing untracked old evidence files and .omo/start-work were present.
- cargo build --release: PASS
  Output: Finished `release` profile [optimized] target(s) in 0.04s
- cargo test --release: PASS
  Output: 10 passed; 0 failed; doc-tests 0 passed.
- ./tests/test_compiler ./target/release/rustcc --chapter 17 --latest-only: FAIL
  Output: Ran 70 tests; FAILED (failures=23)
  Primary baseline root causes:
  - parse error: expected a type specifier ... found Void
  - parse error: expected expression, found Sizeof ("sizeof")

Changes made
- src/ast/ty.rs
  - Made complete-array detection recurse into element type, so `sizeof` on arrays works while arrays of incomplete `void` remain invalid.
- src/ast/expr.rs
  - Added SizeOfExpr and SizeOfType AST nodes.
- src/ast/stmt.rs
  - Changed Return to Option<Expr> so `return;` is representable for void functions.
- src/parse/parser.rs
  - Added `void` to declaration/type-name parsing.
  - Added `sizeof expr` and `sizeof(type)` parsing using abstract declarators for pointer/array type names.
  - Preserved `(void)` empty-parameter marker while allowing `void *param`.
  - Rejects parameter arrays whose raw element type is incomplete before array-to-pointer adjustment.
- src/semantics/resolve.rs and src/semantics/label_loops.rs
  - Threaded new sizeof expression variants and optional returns through existing passes.
- src/semantics/typecheck.rs
  - Validates function signatures, void object restrictions, incomplete array/pointee restrictions, sizeof completeness, void casts, void-return rules, void scalar rejection, void* assignment/conversion/common conditional/equality rules, and rejects pointer arithmetic/subscript/increment on incomplete pointed-to types.
- src/ir/lower.rs
  - Lowers sizeof to unsigned-long constants without lowering/evaluating expression operands.
  - Handles void-return calls/returns/casts/conditionals without materializing a value.
- src/compiler.rs
  - Updated stale in-source unit test from “sizeof unsupported” to “sizeof is lowered without evaluating side effects.”

Verification
- cargo fmt --all -- --check: PASS (exit 0)
- cargo check: PASS
  Output: Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.06s
- cargo build --release: PASS
  Output: Finished `release` profile [optimized] target(s) in 4.67s
- cargo test --release: PASS
  Output: running 10 tests ... ok; test result: ok. 10 passed; 0 failed; doc-tests ok.
- Post-review validate-stage invalid probes: PASS (all rejected)
  - invalid_types/scalar_expressions/not_void.c: exit 1, type error: logical not requires scalar operand
  - invalid_types/void/return_void_as_pointer.c: exit 1, type error: return expression has incompatible type
  - invalid_types/void/void_equality.c: exit 1, type error: cannot compare void expressions
  - invalid_types/incomplete_types/void_array_pointer_in_param_type.c: exit 1, type error: object has void type
- ./tests/test_compiler ./target/release/rustcc --chapter 17 --latest-only: PASS
  Output: Ran 70 tests in 4.475s; OK
- ./tests/test_compiler ./target/release/rustcc --chapter 16 --latest-only: PASS
  Output: Ran 72 tests in 2.330s; OK
  Note: assembler warning in chapter_16 valid/chars/explicit_casts.s: 0x12c shortened to 0x2c; gate still PASS and warning pre-exists this task surface.
- ./tests/test_compiler ./target/release/rustcc --chapter 15 --latest-only: PASS
  Output: Ran 83 tests in 2.591s; OK
- Forbidden bridge scan:
  Command: rg "gcc_array_subset_assembly|source\.contains\(\"\[|-std=c17|system_c_to_assembly|compile_with_system_cc_frontend|sanitize_system_assembly|evaluate_with_system_cc|system_c_syntax_check|should_defer_parse_to_system_frontend|source_has_|likely_parse_error|semantic_error_that_should_parse" src
  Result: no matches (rg exit 1)

Manual QA
- Acceptance example 1:
  Source: int main(void) { return sizeof(int); }
  Command: ./target/release/rustcc /tmp/task46_sizeof.c && /tmp/task46_sizeof
  Observed exit: 4
- Acceptance example 2:
  Source: int main(void) { void *p = (void *)0; return p == 0; }
  Command: ./target/release/rustcc /tmp/task46_voidptr.c && /tmp/task46_voidptr
  Observed exit: 1
- Native stage probe for sizeof no side effects:
  Source: int main(void) { int x = 1; return sizeof(x = 2); }
  Command: ./target/release/rustcc --tacky /tmp/task46_sizeof_probe.c
  Relevant TACKY:
    Copy src Constant(1) dst "main.x.0"
    Copy src Constant(4) dst "const.0"
    type_env contains "const.0": ULong
  Observed: no Constant(2) or assignment lowering for `x = 2`; sizeof lowered to a constant ULong before return truncation to int.
  Post-fix observed exits: sizeof_exit=4, voidptr_exit=1.

Debugging hypotheses and runtime evidence
1. Hypothesis: parser still treats `void` only as the `(void)` empty parameter marker.
   Evidence: baseline Chapter 17 failures included parse errors for `Void`; post-fix Chapter 17 latest-only ran 70 tests OK and acceptance `void *p` exits 1.
   Status: refuted after fix.
2. Hypothesis: `sizeof expr` might evaluate operand side effects during lowering.
   Evidence: manual TACKY probe for `sizeof(x = 2)` contains Constant(4)/ULong and no Constant(2) assignment lowering; unit `parses_sizeof_expression_without_evaluating_it` passes.
   Status: refuted after fix.
3. Hypothesis: void/incomplete pointer operations might be accepted accidentally after allowing `void *` conversions.
   Evidence: Chapter 17 invalid_types suite included incomplete void pointer arithmetic/subscript/void array parameter cases and latest-only gate passed 70 tests OK.
   Status: refuted after fix.

Review lanes
- Context-mining subagent: PASS; searched task plan, docs, chapter 17 tests, OCaml reference, and recent git history; found no missed blocking requirement.
- Security-only subagent: PASS; no HIGH/CRITICAL issues, no active bridge fallback, no new dependencies, no shell injection. Low hygiene note: pre-existing unused `source_path_hint` remains but is not read/branched on.
- QA subagent: PASS; acceptance examples and sizeof side-effect probes passed; wrote supplemental evidence under `.omo/evidence/task-46-ch17-qa/`.
- Initial gate reviewer: REJECT process-only/code-review-artifact gap; also confirmed no staged files, no plan/test/harness diffs, no bridge fingerprints, Ch17 green, and manual probes passed.
- Initial code-quality review: BLOCK; found validate-stage semantic gaps for `!void`, `void == void`, casted void null constants, and nested block-scope function declarations. These were fixed in `src/semantics/typecheck.rs`; post-fix invalid probes and full chapter gates passed. A focused re-review was launched after the fix.
- Local post-fix code-review artifact: PASS; see `.omo/evidence/task-46-ch17-code-review.md` for blocker resolution and overfit/slop review.

Risks / notes
- No commits made.
- No plan checkboxes modified.
- No tests/harness under tests/ modified. Only in-source compiler unit test updated to match the now-supported sizeof behavior.
- No bridge/system-C frontend fallback added; gcc remains only the existing assemble/link helper path.
- The working tree still contains pre-existing unrelated untracked evidence files and .omo/start-work noted before the task; they were not modified intentionally.
